# Marketplace Server Port
PORT=4000

# SQLite Database File Path (mapped to a Docker volume)
DB_PATH=/data/marketplace.db

# Admin key — required for /api/admin/* endpoints (use the same value as the license backend)
ADMIN_SECRET=your_admin_secret_here

# Set to 'true' to hold submitted workflows for admin review before they appear publicly
REQUIRE_APPROVAL=false
